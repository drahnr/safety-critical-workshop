# This works for Linux hosts
[target."thumbv7em-ferrocene.facade-eabi"]
runner = [
    "sh",
    "-c",
    "podman run -v $HOME:$HOME -e LLVM_PROFILE_FILE=$LLVM_PROFILE_FILE --init --rm localhost/ubuntu-qemu-arm qemu-arm-static \"$@\"",
    "--",
]

